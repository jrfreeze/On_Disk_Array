The class OnDiskArray creates a templatized array on a disk that can be written to and read from in a random access mode. 
File is written in binary to permit any data type to be used. Main is just a driver to test it.
